import { makeStyles } from "@material-ui/core/styles";

export const Styles = makeStyles((theme) => ({
  dialogContent: {
    overflow: "hidden",
    "& > div": {
      padding: "20px ",
    },
  },
}));
